# programs
alias e="nvim"
alias cm="chezmoi"

# navigation
alias j="__zoxide_z"
alias cc="cd (fd -t d |fzf)" # cd down the directory structure with fzf

function nf --wraps ls --description 'return latest file in directory'
    {{if eq .chezmoi.os "darwin" }}
    # if darwin, exclude .DS_Store from the list, else don't check for it
    ls -At1 . | sed '/\.DS_Store/d' | sed -n '1p'
    {{else}}
    ls -At1 . | sed -n '1p'
    {{end}}
end

# git
alias gs="git status"

function gcam --wraps git --description 'git commit -a -m'
    git commit -a -m "$argv"
end

# vim: filetype=fish
